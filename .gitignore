build/
.env
config.json
.remember/
/postgres4all
*.test

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/
dist/

# keep generate golden .env fixtures tracked (deterministic test values)
!internal/generate/testdata/golden/*/.env
